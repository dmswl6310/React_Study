import styled from "styled-components";

const PlayerContainer = () => {
  return (
    <>
      <PlayerText>Next Player : </PlayerText>
    </>
  );
};

export default PlayerContainer;

const PlayerText = styled.h3``;
